# Визуальный редактор HTML/CSS

Веб-приложение для создания HTML-страниц с помощью визуального редактора. Создавайте элементы, настраивайте стили, классы и атрибуты, а затем копируйте готовый код.

## Возможности

- ✅ Визуальное создание HTML-элементов
- ✅ Редактирование CSS стилей с автодополнением
- ✅ Добавление CSS классов
- ✅ Настройка атрибутов элементов
- ✅ Вложенность элементов (nested elements)
- ✅ Управление с клавиатуры
- ✅ Генерация готового HTML/CSS кода
- ✅ Копирование кода одним кликом

## Как использовать

### Добавление элементов

1. В левой панели выберите нужный элемент (div, section, h1, p и т.д.)
2. Нажмите на кнопку с названием элемента
3. Элемент появится в области превью

### Редактирование элементов

1. **Выбор элемента**: Кликните на элемент в области превью
2. **Редактирование свойств**: В правой панели "Свойства" вы можете:
   - Изменить текст содержимого
   - Добавить CSS классы
   - Добавить CSS стили
   - Настроить атрибуты (для img, a, input и т.д.)

### Добавление CSS стилей

1. Выберите элемент
2. В разделе "Стили CSS" введите название свойства (например: `margin`)
3. Введите значение (например: `20px`)
4. Нажмите кнопку "Добавить стиль" или клавишу `Enter`

**Автодополнение стилей:**
- Начните вводить название свойства (например: `wi`)
- Появятся подсказки (width, widow и т.д.)
- Нажмите `Tab` или `Enter` для выбора подсказки
- После добавления стиля фокус автоматически вернется на поле "Свойство"

### Добавление CSS классов

1. Выберите элемент
2. В разделе "Классы CSS" введите название класса
3. Нажмите кнопку "Добавить"

### Вложенность элементов

Элементы можно вкладывать друг в друга для создания сложных структур.

**С помощью клавиатуры:**
- `Ctrl + Shift + ↑` - Переместить элемент внутрь предыдущего элемента
- `Ctrl + Shift + ↓` - Вывести элемент из контейнера на уровень выше

**Пример:**
1. Создайте элемент `div`
2. Создайте еще один `div`
3. Выберите второй `div`
4. Нажмите `Ctrl + Shift + ↑` - второй div переместится внутрь первого

### Управление с клавиатуры

#### Навигация по элементам
- `↑` (Стрелка вверх) - Выбрать предыдущий элемент
- `↓` (Стрелка вниз) - Выбрать следующий элемент

#### Перемещение элементов
- `Ctrl + ↑` - Переместить элемент выше (изменить порядок)
- `Ctrl + ↓` - Переместить элемент ниже (изменить порядок)

#### Вложенность элементов
- `Ctrl + Shift + ↑` - Переместить элемент внутрь предыдущего элемента
- `Ctrl + Shift + ↓` - Вывести элемент из контейнера на уровень выше

#### Удаление
- `Delete` - Удалить выбранный элемент

### Копирование кода

1. После создания всех элементов и настройки стилей
2. В правой панели в разделе "Готовый код" будет сгенерированный HTML
3. Нажмите кнопку "📋 Копировать код"
4. Код скопируется в буфер обмена
5. Вставьте его в ваш HTML файл

## Структура проекта

```
page-builder/
├── index.html          # Главная страница
├── css/
│   ├── base.css       # Базовые стили
│   ├── layout.css     # Стили макета
│   ├── components.css # Стили компонентов
│   └── preview.css    # Стили превью
├── js/
│   ├── app.js         # Точка входа
│   ├── elements.js    # Управление элементами
│   ├── dragDrop.js    # Управление клавиатурой
│   ├── codeGenerator.js # Генерация кода
│   └── utils.js       # Утилиты
└── README.md          # Документация
```

## Доступные элементы

### Структура
- `div` - Блочный контейнер
- `section` - Секция
- `header` - Шапка
- `footer` - Подвал
- `main` - Основной контент
- `article` - Статья
- `nav` - Навигация

### Текст
- `h1`, `h2`, `h3` - Заголовки
- `p` - Параграф
- `span` - Инлайн элемент
- `a` - Ссылка

### Формы
- `button` - Кнопка
- `input` - Поле ввода
- `textarea` - Многострочное поле
- `label` - Метка

### Медиа
- `img` - Изображение

### Списки
- `ul` - Маркированный список
- `ol` - Нумерованный список
- `li` - Элемент списка

## Советы

1. **Используйте вложенность**: Создавайте сложные структуры, вкладывая элементы друг в друга
2. **CSS классы vs стили**: Используйте классы для переиспользуемых стилей, inline стили для уникальных
3. **Клавиатурные сокращения**: Используйте горячие клавиши для быстрой работы
4. **Автодополнение**: Используйте Tab для быстрого выбора CSS свойств

## Технические детали

- Чистый JavaScript (без фреймворков)
- Модульная архитектура
- Рекурсивная генерация HTML
- Автоматическая генерация CSS классов из inline стилей

## Лицензия

Свободное использование.
